{
  "name": "food-runner",
  "version": "0.0.1",
  "description": "",
  "main": "app.js",
  "scripts": {
    "start": "cd server && npm install && cd db && npx sequelize db:migrate && npx sequelize db:seed:all && cd .. && cd .. && npm start "
  },
  "license": "ISC",
  "engines": {
    "node": "16.x"
  }
}