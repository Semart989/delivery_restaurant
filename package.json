{
  "name": "food-runner",
  "version": "0.0.1",
  "description": "",
  "main": "app.js",
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "start": "cd server && npm install && npm start"
  },
  "license": "ISC",
  "engines": {
    "node": "14.x"
  }
}